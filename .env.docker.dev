# Entorno Docker de desarrollo
NODE_ENV=development
PORT=4000
JWT_SECRET=replace-with-a-long-random-secret
MFA_ENCRYPTION_KEY=replace-with-a-long-random-secret
POSTGRES_DB=protectora
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres825j
CORS_ORIGINS=http://localhost:8080
ADMIN_BOOTSTRAP_KEY=replace-with-a-bootstrap-key

# Frontend dev
VITE_API_URL=/api
VITE_BACKEND_TARGET=http://backend:4000
VITE_DEV_HOST=0.0.0.0
